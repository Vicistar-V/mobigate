// External API configuration for fetching profile data from your own HTTP endpoint (e.g., PHP on your server)
// Fill these values to enable live fetching. Leaving them empty keeps current mock UI.

export const EXTERNAL_PROFILE_API_URL = ""; // e.g., "https://yourdomain.com/profile.php"
export const EXTERNAL_PROFILE_QUERY_PARAM = "email"; // e.g., "email" | "gsm"
export const EXTERNAL_PROFILE_QUERY_VALUE = ""; // e.g., "user@example.com" or phone number
