

## Voucher Batch Detail -- Statistics & Filter Overhaul (Both Merchant + Sub-Merchant) ✅ DONE

All three layers implemented across both pages:
1. ✅ Top stats count bundles (not cards), 3-col grid, clickable filters
2. ✅ Bundle header shows only available + sold counts
3. ✅ Expanded bundle has Used/Unused pill toggles filtering cards
